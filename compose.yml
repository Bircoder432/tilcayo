services:
  postgres:
    image: postgres:18
    environment:
      POSTGRES_USER: tilcayo
      POSTGRES_PASSWORD: tilcayo
      POSTGRES_DB: tilcayo
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql

volumes:
  postgres_data:
